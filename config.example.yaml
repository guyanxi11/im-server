### 配置模板：复制为 config.yaml 后按本地环境修改，config.yaml 不提交到仓库

server:
  addr: ":8080"

mysql:
  dsn: "root:你的密码@tcp(127.0.0.1:3306)/im_server?charset=utf8mb4&parseTime=True&loc=Local"

jwt:
  secret: "换成随机长字符串"
  expire_hours: 72

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 1
